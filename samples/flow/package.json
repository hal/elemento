{
  "name": "flow-sample",
  "private": true,
  "parcelIgnore": [
    "flow.nocache.js"
  ],
  "source": "src/web/index.html",
  "scripts": {
    "watch": "parcel watch --dist-dir target/gwt/devmode/war/flow",
    "dev": "parcel build --dist-dir target/gwt/devmode/war/flow --no-optimize --public-url /flow",
    "prod": "parcel build --dist-dir target/flow --public-url /elemento/samples/flow/"
  },
  "devDependencies": {
    "@parcel/reporter-cli": "^2.12.0",
    "parcel": "^2.12.0",
    "parcel-resolver-ignore": "^2.2.0"
  },
  "dependencies": {
    "@patternfly/patternfly": "^5.2.1",
    "@popperjs/core": "^2.11.8"
  }
}
